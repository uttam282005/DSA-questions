#include <bits/stdc++.h>
using namespace std;

int binary_search(vector<int> &arr, int n, int key) {
  int low = 0;
  int high = n - 1;
  int mid;
  while (high - low > 1) {
  }
}

int main() { return 0; }
